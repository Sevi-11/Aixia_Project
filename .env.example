# Host ports for `docker compose up`. Only these two ever need to change if a
# port is already taken by another project on your machine — no rebuild required,
# just edit this file (copy it to `.env` first) and run `docker compose up -d`.
AIXIA_BACKEND_PORT=8001
AIXIA_FRONTEND_PORT=3000
