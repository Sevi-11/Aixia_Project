Django==6.0.7
djangorestframework==3.17.1
django-cors-headers==4.9.0
python-dotenv==1.1.1

# psycopg 3. Django 6 prefers it over psycopg2 when both are present, and
# langchain-postgres requires it, so shipping one driver avoids the confusion
# of two.
psycopg[binary]==3.3.5

langchain==1.3.11
langchain-core==1.6.2
langchain-groq==1.1.3
langchain-text-splitters==1.1.2

# Embeddings are served by the Gemini API and stored as pgvector rows in the
# application's own Postgres. This replaces chromadb + sentence-transformers +
# langchain-huggingface, which between them pulled in torch: ~3 GB of image and
# several hundred MB resident per worker, well past what a free-tier container
# allows.
langchain-google-genai==4.4.0
langchain-postgres==0.0.17

PyMuPDF==1.28.0

# Uploaded PDFs go to S3-compatible object storage (Supabase Storage) in
# production. A PaaS container's filesystem is ephemeral, so anything written
# to MEDIA_ROOT vanishes on the next deploy -- silently, leaving database rows
# pointing at files that no longer exist.
django-storages[s3]==1.14.6

gunicorn==26.0.0
uvicorn==0.35.0
whitenoise==6.12.0
